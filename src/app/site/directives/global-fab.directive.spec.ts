import { GlobalFabDirective } from './global-fab.directive'

describe('GlobalFabDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalFabDirective()
    expect(directive).toBeTruthy()
  })
})
